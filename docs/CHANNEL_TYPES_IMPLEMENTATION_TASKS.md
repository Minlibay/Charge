# Задачи для реализации типов каналов

## Обзор

Этот документ содержит детальные задачи для реализации функционала каналов типов:
- **ANNOUNCEMENT CHANNELS** (Каналы объявлений)
- **FORUM CHANNELS** (Форумные каналы)
- **EVENT CHANNELS** (Каналы событий)

---

## 📢 ANNOUNCEMENT CHANNELS

### Фаза 1: Backend - Модели и миграции

#### Задача 1.1: Создание модели AnnouncementCrossPost

**Приоритет**: Высокий  
**Оценка**: 2 часа

**Подзадачи:**
1. Добавить модель `AnnouncementCrossPost` в `backend/app/models/chat.py`:
   ```python
   class AnnouncementCrossPost(Base):
       __tablename__ = "announcement_cross_posts"
       
       id: Mapped[int] = mapped_column(primary_key=True)
       original_message_id: Mapped[int] = mapped_column(
           ForeignKey("messages.id", ondelete="CASCADE"), nullable=False
       )
       cross_posted_message_id: Mapped[int] = mapped_column(
           ForeignKey("messages.id", ondelete="CASCADE"), nullable=False
       )
       target_channel_id: Mapped[int] = mapped_column(
           ForeignKey("channels.id", ondelete="CASCADE"), nullable=False
       )
       created_at: Mapped[datetime] = mapped_column(
           DateTime(timezone=True), server_default=func.now(), nullable=False
       )
       
       original_message: Mapped["Message"] = relationship(foreign_keys=[original_message_id])
       cross_posted_message: Mapped["Message"] = relationship(foreign_keys=[cross_posted_message_id])
       target_channel: Mapped["Channel"] = relationship()
   ```

2. Добавить индексы:
   - `ix_cross_posts_original` на `original_message_id`
   - `ix_cross_posts_cross_posted` на `cross_posted_message_id`
   - `ix_cross_posts_target_channel` на `target_channel_id`

3. Обновить `__init__.py` для экспорта

**Критерии приемки:**
- ✅ Модель создана и корректно определена
- ✅ Relationships настроены
- ✅ Индексы добавлены

---

#### Задача 1.2: Миграция для AnnouncementCrossPost

**Приоритет**: Высокий  
**Оценка**: 1 час

**Подзадачи:**
1. Создать Alembic миграцию:
   ```bash
   alembic revision -m "add_announcement_cross_posts"
   ```

2. В миграции создать таблицу `announcement_cross_posts` с полями из задачи 1.1

3. Добавить индексы

4. Протестировать миграцию (upgrade/downgrade)

**Критерии приемки:**
- ✅ Миграция создана и работает
- ✅ Rollback работает корректно

---

### Фаза 2: Backend - API endpoints

#### Задача 2.1: API для создания объявлений

**Приоритет**: Высокий  
**Оценка**: 3 часа

**Подзадачи:**
1. Обновить `POST /channels/{channel_id}/messages` в `backend/app/api/messages.py`:
   - Проверять тип канала (ANNOUNCEMENTS)
   - Проверять право `PUBLISH_ANNOUNCEMENTS`
   - Добавить флаг `is_announcement` в ответ

2. Создать endpoint `POST /channels/{channel_id}/announcements`:
   - Создание объявления с автоматическим форматированием
   - Валидация прав
   - Возврат созданного сообщения

**Критерии приемки:**
- ✅ Endpoint работает
- ✅ Проверки прав реализованы
- ✅ Валидация работает

---

#### Задача 2.2: API для cross-posting

**Приоритет**: Высокий  
**Оценка**: 5 часов

**Подзадачи:**
1. Создать endpoint `POST /channels/{channel_id}/announcements/{message_id}/cross-post`:
   - Принимать список `target_channel_ids`
   - Проверять права на публикацию в целевые каналы
   - Создавать копии сообщения в целевых каналах
   - Создавать записи `AnnouncementCrossPost`
   - Возвращать список созданных копий

2. Создать endpoint `GET /channels/{channel_id}/announcements/{message_id}/cross-posts`:
   - Возвращать список каналов, куда опубликовано объявление
   - Информацию о копиях

3. Создать endpoint `DELETE /channels/{channel_id}/announcements/{message_id}/cross-posts/{target_channel_id}`:
   - Удаление cross-post из конкретного канала
   - Удаление связанного сообщения

**Критерии приемки:**
- ✅ Cross-posting работает
- ✅ Связи между сообщениями создаются
- ✅ Удаление работает корректно

---

#### Задача 2.3: Синхронизация реакций

**Приоритет**: Средний  
**Оценка**: 4 часа

**Подзадачи:**
1. Обновить `POST /channels/{channel_id}/messages/{message_id}/reactions`:
   - Проверять, является ли сообщение частью cross-post
   - Если да, синхронизировать реакцию со всеми копиями
   - Обновлять все связанные сообщения

2. Обновить `DELETE /channels/{channel_id}/messages/{message_id}/reactions`:
   - Аналогично для удаления реакций

3. Добавить фоновую задачу для синхронизации (опционально)

**Критерии приемки:**
- ✅ Реакции синхронизируются
- ✅ Производительность приемлема

---

### Фаза 3: Frontend - UI для объявлений

#### Задача 3.1: Компоненты для объявлений

**Приоритет**: Высокий  
**Оценка**: 6 часов

**Подзадачи:**
1. Создать `frontend/src/components/AnnouncementMessage.tsx`:
   - Выделенный дизайн для объявлений
   - Баннер "ОБЪЯВЛЕНИЕ"
   - Отображение источника (если cross-post)

2. Обновить `MessageList.tsx`:
   - Определять тип сообщения (объявление)
   - Использовать `AnnouncementMessage` для объявлений

3. Создать `CrossPostDialog.tsx`:
   - Выбор каналов для публикации
   - Список доступных каналов
   - Кнопка публикации

**Критерии приемки:**
- ✅ UI интуитивен
- ✅ Объявления визуально выделены
- ✅ Cross-posting работает

---

#### Задача 3.2: Интеграция с store

**Приоритет**: Высокий  
**Оценка**: 3 часа

**Подзадачи:**
1. Обновить `frontend/src/services/api.ts`:
   - `createAnnouncement(channelId, content)`
   - `crossPostAnnouncement(channelId, messageId, targetChannelIds)`
   - `getCrossPosts(channelId, messageId)`
   - `deleteCrossPost(channelId, messageId, targetChannelId)`

2. Обновить `workspaceStore.ts`:
   - Обработка событий объявлений
   - Обновление UI при cross-posting

**Критерии приемки:**
- ✅ API методы работают
- ✅ Store обновляется корректно

---

## 💬 FORUM CHANNELS

### Фаза 1: Backend - Модели и миграции

#### Задача 1.1: Создание моделей форума

**Приоритет**: Высокий  
**Оценка**: 4 часа

**Подзадачи:**
1. Добавить модель `ForumPost` в `backend/app/models/chat.py`:
   - Поля: id, channel_id, message_id, title, author_id, is_pinned, is_archived, is_locked, reply_count, last_reply_at, last_reply_by_id, created_at, updated_at
   - Relationships: channel, message, author, last_reply_by, tags

2. Добавить модель `ForumPostTag`:
   - Поля: id, post_id, tag_name, created_at
   - UniqueConstraint: (post_id, tag_name)

3. Добавить модель `ForumChannelTag`:
   - Поля: id, channel_id, name, color, emoji, created_at
   - UniqueConstraint: (channel_id, name)

4. Добавить индексы для производительности

**Критерии приемки:**
- ✅ Все модели созданы
- ✅ Relationships настроены
- ✅ Constraints добавлены

---

#### Задача 1.2: Миграции для форума

**Приоритет**: Высокий  
**Оценка**: 2 часа

**Подзадачи:**
1. Создать Alembic миграцию:
   ```bash
   alembic revision -m "add_forum_channels"
   ```

2. Создать таблицы: `forum_posts`, `forum_post_tags`, `forum_channel_tags`

3. Добавить индексы

4. Протестировать миграцию

**Критерии приемки:**
- ✅ Миграция работает
- ✅ Rollback работает

---

### Фаза 2: Backend - API endpoints

#### Задача 2.1: API для постов

**Приоритет**: Высокий  
**Оценка**: 8 часов

**Подзадачи:**
1. Создать `POST /channels/{channel_id}/posts`:
   - Создание поста (первое сообщение становится постом)
   - Валидация прав `CREATE_FORUM_POSTS`
   - Создание записи `ForumPost`
   - Возврат поста с метаданными

2. Создать `GET /channels/{channel_id}/posts`:
   - Список постов с пагинацией
   - Сортировка (по дате, активности, популярности)
   - Фильтрация по тегам
   - Фильтрация по статусу (активен, закреплен, архивирован)
   - Возврат метаданных (количество ответов, последний ответ)

3. Создать `GET /channels/{channel_id}/posts/{post_id}`:
   - Получение поста с ответами
   - Загрузка всех сообщений в ветке
   - Метаданные поста

4. Создать `PATCH /channels/{channel_id}/posts/{post_id}`:
   - Обновление заголовка поста
   - Обновление тегов
   - Проверка прав (автор или модератор)

5. Создать `DELETE /channels/{channel_id}/posts/{post_id}`:
   - Удаление поста
   - Проверка прав `MODERATE_FORUM_POSTS`
   - Каскадное удаление ответов

**Критерии приемки:**
- ✅ Все endpoints работают
- ✅ Проверки прав реализованы
- ✅ Пагинация работает

---

#### Задача 2.2: API для управления постами

**Приоритет**: Высокий  
**Оценка**: 4 часа

**Подзадачи:**
1. Создать `POST /channels/{channel_id}/posts/{post_id}/pin`:
   - Закрепление поста
   - Проверка прав

2. Создать `DELETE /channels/{channel_id}/posts/{post_id}/pin`:
   - Открепление поста

3. Создать `POST /channels/{channel_id}/posts/{post_id}/archive`:
   - Архивация поста
   - Проверка прав

4. Создать `POST /channels/{channel_id}/posts/{post_id}/unarchive`:
   - Разархивация поста

5. Создать `POST /channels/{channel_id}/posts/{post_id}/lock`:
   - Блокировка поста (запрет новых ответов)
   - Проверка прав

6. Создать `POST /channels/{channel_id}/posts/{post_id}/unlock`:
   - Разблокировка поста

**Критерии приемки:**
- ✅ Все операции работают
- ✅ Проверки прав реализованы

---

#### Задача 2.3: API для тегов

**Приоритет**: Средний  
**Оценка**: 3 часа

**Подзадачи:**
1. Создать `POST /channels/{channel_id}/tags`:
   - Создание тега канала
   - Проверка прав `MANAGE_CHANNEL`
   - Валидация названия и цвета

2. Создать `GET /channels/{channel_id}/tags`:
   - Список тегов канала

3. Создать `PATCH /channels/{channel_id}/tags/{tag_id}`:
   - Обновление тега

4. Создать `DELETE /channels/{channel_id}/tags/{tag_id}`:
   - Удаление тега

5. Создать `POST /channels/{channel_id}/posts/{post_id}/tags`:
   - Добавление тегов к посту
   - Валидация тегов

6. Создать `DELETE /channels/{channel_id}/posts/{post_id}/tags/{tag_name}`:
   - Удаление тега из поста

**Критерии приемки:**
- ✅ Управление тегами работает
- ✅ Валидация работает

---

#### Задача 2.4: Логика обновления метаданных

**Приоритет**: Высокий  
**Оценка**: 4 часа

**Подзадачи:**
1. Создать функцию `update_forum_post_metadata(post_id, db)`:
   - Подсчет количества ответов
   - Обновление `last_reply_at`
   - Обновление `last_reply_by_id`
   - Обновление `reply_count`

2. Вызывать функцию при:
   - Создании нового ответа на пост
   - Удалении ответа
   - Обновлении ответа

3. Добавить триггеры или события для автоматического обновления

**Критерии приемки:**
- ✅ Метаданные обновляются автоматически
- ✅ Производительность приемлема

---

### Фаза 3: Frontend - UI для форума

#### Задача 3.1: Компоненты списка постов

**Приоритет**: Высокий  
**Оценка**: 8 часов

**Подзадачи:**
1. Создать `frontend/src/components/ForumPostList.tsx`:
   - Список постов в виде карточек
   - Отображение заголовка, автора, даты
   - Количество ответов
   - Последний ответ
   - Теги поста
   - Индикаторы (закреплен, архивирован, заблокирован)

2. Создать `frontend/src/components/ForumPostCard.tsx`:
   - Карточка отдельного поста
   - Клик открывает детальный вид

3. Добавить фильтры и сортировку:
   - Фильтр по тегам
   - Сортировка (дата, активность, популярность)
   - Поиск по постам

**Критерии приемки:**
- ✅ Список постов отображается
- ✅ Фильтры работают
- ✅ UI интуитивен

---

#### Задача 3.2: Компоненты детального вида поста

**Приоритет**: Высокий  
**Оценка**: 6 часов

**Подзадачи:**
1. Создать `frontend/src/components/ForumPostView.tsx`:
   - Детальный вид поста
   - Заголовок поста
   - Первое сообщение (пост)
   - Список ответов
   - Форма для ответа

2. Обновить `MessageList.tsx`:
   - Поддержка отображения поста и ответов
   - Визуальное разделение поста и ответов

3. Добавить управление постом:
   - Кнопки: закрепить, архивировать, заблокировать
   - Редактирование заголовка
   - Управление тегами

**Критерии приемки:**
- ✅ Детальный вид работает
- ✅ Ответы отображаются корректно
- ✅ Управление работает

---

#### Задача 3.3: Компоненты создания поста

**Приоритет**: Высокий  
**Оценка**: 4 часа

**Подзадачи:**
1. Создать `frontend/src/components/CreateForumPostDialog.tsx`:
   - Поле заголовка
   - Поле содержимого (первое сообщение)
   - Выбор тегов
   - Кнопка создания

2. Интегрировать с API:
   - Создание поста
   - Создание первого сообщения
   - Обновление списка постов

**Критерии приемки:**
- ✅ Создание поста работает
- ✅ Валидация работает

---

## 📅 EVENT CHANNELS

### Фаза 1: Backend - Модели и миграции

#### Задача 1.1: Создание моделей событий

**Приоритет**: Средний  
**Оценка**: 4 часа

**Подзадачи:**
1. Добавить модель `Event` в `backend/app/models/chat.py`:
   - Поля: id, channel_id, message_id, title, description, organizer_id, start_time, end_time, location, image_url, external_url, status, created_at, updated_at
   - Relationships: channel, message, organizer, participants, reminders

2. Добавить модель `EventParticipant`:
   - Поля: id, event_id, user_id, rsvp_status, joined_at
   - UniqueConstraint: (event_id, user_id)

3. Добавить модель `EventReminder`:
   - Поля: id, event_id, user_id, reminder_time, sent, sent_at, created_at
   - UniqueConstraint: (event_id, user_id, reminder_time)

4. Добавить индексы

**Критерии приемки:**
- ✅ Все модели созданы
- ✅ Relationships настроены

---

#### Задача 1.2: Миграции для событий

**Приоритет**: Средний  
**Оценка**: 2 часа

**Подзадачи:**
1. Создать Alembic миграцию:
   ```bash
   alembic revision -m "add_event_channels"
   ```

2. Создать таблицы: `events`, `event_participants`, `event_reminders`

3. Добавить индексы

**Критерии приемки:**
- ✅ Миграция работает

---

### Фаза 2: Backend - API endpoints

#### Задача 2.1: API для событий

**Приоритет**: Средний  
**Оценка**: 8 часов

**Подзадачи:**
1. Создать `POST /channels/{channel_id}/events`:
   - Создание события
   - Валидация прав `CREATE_EVENTS`
   - Валидация дат и времени
   - Создание напоминаний (опционально)

2. Создать `GET /channels/{channel_id}/events`:
   - Список событий
   - Фильтрация по статусу
   - Фильтрация по дате
   - Сортировка

3. Создать `GET /channels/{channel_id}/events/{event_id}`:
   - Получение события
   - Информация об участниках
   - Напоминания

4. Создать `PATCH /channels/{channel_id}/events/{event_id}`:
   - Обновление события
   - Проверка прав `MANAGE_EVENTS`
   - Валидация

5. Создать `DELETE /channels/{channel_id}/events/{event_id}`:
   - Удаление/отмена события
   - Проверка прав

**Критерии приемки:**
- ✅ Все endpoints работают
- ✅ Валидация работает

---

#### Задача 2.2: API для участников событий

**Приоритет**: Средний  
**Оценка**: 4 часа

**Подзадачи:**
1. Создать `POST /channels/{channel_id}/events/{event_id}/rsvp`:
   - Изменение RSVP статуса (yes, no, maybe, interested)
   - Создание/обновление записи `EventParticipant`

2. Создать `GET /channels/{channel_id}/events/{event_id}/participants`:
   - Список участников
   - Группировка по RSVP статусу
   - Количество по каждому статусу

3. Создать `DELETE /channels/{channel_id}/events/{event_id}/rsvp`:
   - Удаление RSVP (отказ от участия)

**Критерии приемки:**
- ✅ RSVP работает
- ✅ Список участников отображается

---

#### Задача 2.3: API для напоминаний

**Приоритет**: Низкий  
**Оценка**: 3 часа

**Подзадачи:**
1. Создать `POST /channels/{channel_id}/events/{event_id}/reminders`:
   - Создание напоминания
   - Валидация времени напоминания

2. Создать `GET /channels/{channel_id}/events/{event_id}/reminders`:
   - Список напоминаний пользователя

3. Создать `DELETE /channels/{channel_id}/events/{event_id}/reminders/{reminder_id}`:
   - Удаление напоминания

**Критерии приемки:**
- ✅ Напоминания создаются
- ✅ Управление работает

---

#### Задача 2.4: Система автоматического обновления статусов

**Приоритет**: Средний  
**Оценка**: 4 часа

**Подзадачи:**
1. Создать фоновую задачу (Celery или аналогичная):
   - Проверка событий каждые N минут
   - Обновление статуса: scheduled -> ongoing (при start_time)
   - Обновление статуса: ongoing -> completed (при end_time)

2. Создать endpoint для ручного обновления:
   - `POST /channels/{channel_id}/events/{event_id}/update-status`

3. Добавить логирование изменений статусов

**Критерии приемки:**
- ✅ Статусы обновляются автоматически
- ✅ Фоновая задача работает стабильно

---

#### Задача 2.5: Система напоминаний

**Приоритет**: Низкий  
**Оценка**: 6 часов

**Подзадачи:**
1. Создать фоновую задачу для отправки напоминаний:
   - Проверка `EventReminder` с `sent=False` и `reminder_time <= now()`
   - Отправка уведомлений (push, email)
   - Обновление `sent=True` и `sent_at`

2. Интегрировать с системой уведомлений:
   - Push уведомления
   - Email уведомления (опционально)

3. Добавить логирование отправки

**Критерии приемки:**
- ✅ Напоминания отправляются
- ✅ Уведомления работают

---

#### Задача 2.6: Экспорт в календарь

**Приоритет**: Низкий  
**Оценка**: 3 часа

**Подзадачи:**
1. Создать `GET /channels/{channel_id}/events/{event_id}/calendar.ics`:
   - Генерация iCal файла
   - Формат RFC 5545

2. Создать `GET /channels/{channel_id}/events/calendar.ics`:
   - Экспорт всех событий канала

3. Добавить поддержку Google Calendar, Outlook (опционально)

**Критерии приемки:**
- ✅ iCal файлы генерируются
- ✅ Импорт в календари работает

---

### Фаза 3: Frontend - UI для событий

#### Задача 3.1: Компоненты календаря

**Приоритет**: Средний  
**Оценка**: 8 часов

**Подзадачи:**
1. Создать `frontend/src/components/EventCalendar.tsx`:
   - Календарное представление событий
   - Месячный вид
   - Недельный вид (опционально)
   - Дневной вид (опционально)

2. Интегрировать библиотеку календаря (react-big-calendar или аналогичная)

3. Добавить навигацию по месяцам

**Критерии приемки:**
- ✅ Календарь отображается
- ✅ События видны на календаре
- ✅ Навигация работает

---

#### Задача 3.2: Компоненты списка событий

**Приоритет**: Средний  
**Оценка**: 6 часов

**Подзадачи:**
1. Создать `frontend/src/components/EventList.tsx`:
   - Список событий
   - Фильтрация по статусу
   - Фильтрация по дате
   - Сортировка

2. Создать `frontend/src/components/EventCard.tsx`:
   - Карточка события
   - Основная информация
   - Количество участников
   - RSVP статус пользователя

**Критерии приемки:**
- ✅ Список отображается
- ✅ Фильтры работают

---

#### Задача 3.3: Компоненты создания/редактирования события

**Приоритет**: Средний  
**Оценка**: 6 часов

**Подзадачи:**
1. Создать `frontend/src/components/CreateEventDialog.tsx`:
   - Поле названия
   - Поле описания
   - Выбор даты и времени начала
   - Выбор даты и времени окончания (опционально)
   - Поле местоположения
   - Загрузка изображения
   - Поле внешней ссылки
   - Настройки напоминаний

2. Создать `frontend/src/components/EditEventDialog.tsx`:
   - Аналогично CreateEventDialog
   - Загрузка существующих данных

**Критерии приемки:**
- ✅ Создание работает
- ✅ Редактирование работает
- ✅ Валидация работает

---

#### Задача 3.4: Компоненты детального вида события

**Приоритет**: Средний  
**Оценка**: 5 часов

**Подзадачи:**
1. Создать `frontend/src/components/EventView.tsx`:
   - Детальный вид события
   - Вся информация о событии
   - Список участников
   - RSVP интерфейс
   - Кнопка экспорта в календарь

2. Добавить RSVP интерфейс:
   - Кнопки: Да, Нет, Возможно, Заинтересован
   - Отображение текущего статуса

**Критерии приемки:**
- ✅ Детальный вид работает
- ✅ RSVP работает

---

## Общие задачи

### Backend

#### Задача G.1: Обновление валидации типов каналов

**Приоритет**: Высокий  
**Оценка**: 2 часа

**Подзадачи:**
1. Обновить `create_channel` в `backend/app/api/rooms.py`:
   - Валидация типа канала
   - Проверка разрешений для создания конкретного типа

2. Обновить проверки в других endpoints

**Критерии приемки:**
- ✅ Валидация работает
- ✅ Проверки прав работают

---

#### Задача G.2: Обновление workspace events

**Приоритет**: Высокий  
**Оценка**: 3 часа

**Подзадачи:**
1. Обновить `backend/app/services/workspace_events.py`:
   - События для объявлений
   - События для постов форума
   - События для событий

2. Добавить обработку в WebSocket

**Критерии приемки:**
- ✅ События отправляются
- ✅ Real-time обновления работают

---

### Frontend

#### Задача G.3: Обновление CreateChannelDialog

**Приоритет**: Высокий  
**Оценка**: 2 часа

**Подзадачи:**
1. Обновить `frontend/src/components/dialogs/CreateChannelDialog.tsx`:
   - Добавить описание для каждого типа канала
   - Визуальные индикаторы типов

**Критерии приемки:**
- ✅ Все типы доступны
- ✅ UI понятен

---

#### Задача G.4: Обновление ChannelSidebar

**Приоритет**: Высокий  
**Оценка**: 3 часа

**Подзадачи:**
1. Обновить `frontend/src/components/ChannelSidebar.tsx`:
   - Разные иконки для разных типов
   - Визуальное разделение типов

**Критерии приемки:**
- ✅ Типы визуально различимы
- ✅ UI понятен

---

## Тестирование

### Задача T.1: Unit тесты

**Приоритет**: Средний  
**Оценка**: 12 часов

**Подзадачи:**
1. Тесты для моделей
2. Тесты для API endpoints
3. Тесты для бизнес-логики
4. Покрытие > 80%

---

### Задача T.2: Integration тесты

**Приоритет**: Средний  
**Оценка**: 8 часов

**Подзадачи:**
1. E2E тесты для каждого типа канала
2. Тесты cross-posting
3. Тесты форума
4. Тесты событий

---

## Итоговая оценка времени

| Тип канала | Backend | Frontend | Тестирование | Итого |
|------------|---------|----------|--------------|-------|
| ANNOUNCEMENTS | 16 часов | 9 часов | 4 часа | **29 часов** |
| FORUMS | 23 часа | 18 часов | 6 часов | **47 часов** |
| EVENTS | 30 часов | 25 часов | 8 часов | **63 часа** |
| Общие задачи | 5 часов | 5 часов | 2 часа | **12 часов** |
| **ИТОГО** | **74 часа** | **57 часов** | **20 часов** | **151 час** |

---

## Порядок выполнения (рекомендуемый)

1. **Неделя 1-2**: ANNOUNCEMENT CHANNELS (Backend + Frontend)
2. **Неделя 3-4**: FORUM CHANNELS (Backend)
3. **Неделя 5-6**: FORUM CHANNELS (Frontend)
4. **Неделя 7-9**: EVENT CHANNELS (Backend + Frontend)
5. **Неделя 10**: Тестирование и доработки

---

## Важные замечания

1. **Обратная совместимость**: Все изменения должны быть обратно совместимы
2. **Миграции данных**: При создании миграций убедиться, что существующие данные не потеряются
3. **Производительность**: Индексы критичны для больших каналов
4. **Безопасность**: Все проверки прав должны быть на backend
5. **UX**: Интерфейс должен быть интуитивным


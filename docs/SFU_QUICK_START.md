# Быстрый старт: SFU для Charge

## Что уже сделано автоматически ✅

1. ✅ Создана структура SFU сервера (`sfu-server/`)
2. ✅ Реализован базовый Mediasoup сервер
3. ✅ Создан Dockerfile для SFU
4. ✅ Обновлен `docker-compose.yml` с новым сервисом `sfu`
5. ✅ Создана документация

## Что нужно сделать ВРУЧНУЮ 🔧

### Шаг 1: Установить зависимости SFU сервера

**Выполнить в терминале:**

```bash
cd sfu-server
npm install
```

**Время:** 2-5 минут  
**Что делает:** Устанавливает все Node.js зависимости (mediasoup, express, ws и т.д.)

### Шаг 2: Добавить переменные окружения

**Добавить в файл `.env` в корне проекта:**

```env
# SFU Configuration
SFU_API_KEY=your-secret-api-key-change-this-to-random-string
# В Docker Compose SFU работает в host-сети, поэтому бэкенд подключается через host.docker.internal
SFU_SERVER_URL=http://host.docker.internal:3001
SFU_WS_URL=ws://host.docker.internal:3001
SFU_ANNOUNCED_IP=45.144.66.105
SFU_RTC_MIN_PORT=40000
SFU_RTC_MAX_PORT=49999
SFU_CORS_ORIGIN=http://localhost:80,https://charvi.ru
```

**Важно:**
- `SFU_API_KEY` – **обязательно** замените на случайную строку (например, `openssl rand -hex 32`) и используйте её и в бекенде, и в SFU.
- `SFU_SERVER_URL`/`SFU_WS_URL` указываем на `host.docker.internal`, потому что контейнер `sfu` использует `network_mode: host` и доступен со стороны других контейнеров по IP хоста.
- Остальные переменные уже настроены правильно.

### Шаг 3: Собрать и запустить SFU

**Выполнить в терминале (из корня проекта):**

```bash
# Собрать Docker образ
docker-compose build sfu

# Запустить SFU сервер
docker-compose up -d sfu

# Проверить логи
docker-compose logs -f sfu
```

**Ожидаемый результат:**
- В логах должно быть: `[HTTP] Server listening on 0.0.0.0:3001`
- Health check работает: `curl http://localhost:3001/health`

### Шаг 4: Проверить работу

**Выполнить в терминале:**

```bash
# Health check
curl http://localhost:3001/health

# Проверка из контейнера бэкенда (подтверждаем доступ к SFU по API-ключу)
docker compose exec api curl -H "x-api-key:$SFU_API_KEY" http://host.docker.internal:3001/api/rooms

# Должен вернуть: {"status":"ok","timestamp":"..."}
```

**Ожидаемый результат:**
- В логах должно быть: `[HTTP] Server listening on 0.0.0.0:3001`
- Health check работает: `curl http://localhost:3001/health`
- Вызов из контейнера `api` возвращает код 200 и список комнат (даже если массив пустой) — значит, бэкенд видит SFU и API-ключ совпадает.

## Следующие этапы (будут выполнены автоматически)

После успешного запуска SFU сервера, я продолжу с:
- Этап 2: Интеграция с бэкендом (SFU Manager, обновление VoiceSignalManager)
- Этап 3: Обновление клиента (SFUVoiceClient, рефакторинг)
- Этап 4: Тестирование

## Если что-то пошло не так

### Проблема: `npm install` не работает

**Решение:**
- Убедитесь, что Node.js 18+ установлен: `node --version`
- Убедитесь, что npm установлен: `npm --version`
- Если проблемы, попробуйте: `npm install --legacy-peer-deps`

### Проблема: Docker build не работает

**Решение:**
- Проверьте логи: `docker-compose build sfu 2>&1 | tee build.log`
- Убедитесь, что Docker запущен
- Проверьте, что есть доступ к интернету

### Проблема: Порты заняты

**Решение:**
- Проверьте: `netstat -tuln | grep -E '3000|3001'`
- Если заняты, измените порты в `docker-compose.yml` и `.env`

## Подробная инструкция

См. `docs/SFU_SETUP_INSTRUCTIONS.md` для детальной инструкции со всеми шагами.

## Готово к продолжению?

После выполнения шагов 1-4, сообщите мне, и я продолжу с интеграцией бэкенда и клиента! 🚀


# План функционала для типов каналов

## Обзор

В системе определены следующие типы каналов:
- **TEXT** - текстовые каналы (✅ реализованы)
- **VOICE** - голосовые каналы (✅ реализованы)
- **STAGE** - сценические каналы (✅ реализованы)
- **ANNOUNCEMENTS** - каналы объявлений (❌ не реализованы)
- **FORUMS** - форумные каналы (❌ не реализованы)
- **EVENTS** - каналы событий (❌ не реализованы)

Данный документ описывает функционал для каждого типа канала, его назначение, особенности и план реализации.

---

## 1. TEXT CHANNELS (Текстовые каналы)

### Назначение
Основной тип канала для текстового общения. Поддерживает все базовые функции обмена сообщениями.

### Текущий функционал ✅
- Отправка и получение сообщений
- Редактирование и удаление сообщений
- Вложения (файлы, изображения)
- Реакции на сообщения
- Ответы на сообщения (replies)
- Ветки обсуждений (threads)
- Закрепленные сообщения (pinned messages)
- Поиск по сообщениям
- Модерация сообщений
- Slowmode
- Topic (описание канала)
- NSFW пометка
- Приватность
- Архивация

### Разрешения
- `VIEW` - просмотр канала
- `SEND_MESSAGES` - отправка сообщений
- `MANAGE_MESSAGES` - управление сообщениями
- `MANAGE_CHANNEL` - управление каналом
- `MANAGE_PERMISSIONS` - управление правами

### Статус
✅ **Полностью реализовано**

---

## 2. VOICE CHANNELS (Голосовые каналы)

### Назначение
Каналы для голосового общения в реальном времени через WebRTC. Участники могут подключаться, говорить, включать/выключать микрофон и наушники.

### Текущий функционал ✅
- Подключение к голосовому каналу
- Отключение от канала
- Управление микрофоном (mute/unmute)
- Управление наушниками (deafen/undeafen)
- Отображение списка участников
- Индикация активности (говорит/не говорит)
- Качество соединения (MOS, jitter, RTT, packet loss)
- WebRTC signaling через TURN сервер

### Разрешения
- `VIEW` - просмотр канала
- `CONNECT` - подключение к каналу
- `SPEAK` - возможность говорить
- `MANAGE_CHANNEL` - управление каналом
- `MANAGE_PERMISSIONS` - управление правами

### Особенности
- Не поддерживает текстовые сообщения
- Требует WebRTC инфраструктуру
- Интеграция с TURN сервером для NAT traversal

### Статус
✅ **Полностью реализовано**

---

## 3. STAGE CHANNELS (Сценические каналы)

### Назначение
Каналы для проведения презентаций, лекций и выступлений. Один или несколько спикеров выступают перед аудиторией слушателей. Поддерживает управление спикерами, приглашения на сцену, поднятие руки.

### Текущий функционал ✅
- Статусы участников:
  - `listener` - слушатель (только слушает)
  - `invited` - приглашен на сцену
  - `requesting` - запросил доступ (поднял руку)
  - `backstage` - за кулисами (готов к выступлению)
  - `live` - выступает на сцене
  - `muted` - приглушен модератором
- Поднятие руки для запроса доступа
- Приглашение на сцену модераторами
- Управление спикерами (mute/unmute, удаление со сцены)
- Отображение активных спикеров и слушателей

### Разрешения
- `VIEW` - просмотр канала
- `CONNECT` - подключение к каналу (как слушатель)
- `SPEAK` - возможность говорить (автоматически при приглашении)
- `START_STAGE` - запуск сценического канала
- `MANAGE_STAGE` - управление сценой (приглашения, управление спикерами)
- `MANAGE_CHANNEL` - управление каналом
- `MANAGE_PERMISSIONS` - управление правами

### Особенности
- Требует модератора для управления сценой
- Слушатели не могут говорить без приглашения
- Поддержка поднятия руки для запроса доступа
- Возможность приглушения спикеров

### Статус
✅ **Полностью реализовано**

---

## 4. ANNOUNCEMENT CHANNELS (Каналы объявлений)

### Назначение
Специализированные каналы для публикации важных объявлений. Сообщения в таких каналах могут быть опубликованы в другие каналы (follow/cross-post). Обычно только модераторы могут публиковать сообщения, но все могут их читать.

### Планируемый функционал ❌

#### Базовый функционал
- Отправка сообщений (только с правом `PUBLISH_ANNOUNCEMENTS`)
- Автоматическое форматирование объявлений
- Индикатор "Объявление" в интерфейсе
- Ограничение на редактирование (только автор или модераторы)
- Ограничение на удаление (только модераторы)

#### Cross-posting (публикация в другие каналы)
- Возможность выбрать каналы для публикации объявления
- Автоматическая публикация в связанные каналы
- Синхронизация реакций между оригиналом и копиями
- Отображение источника объявления
- Управление подписками на каналы объявлений

#### UI/UX особенности
- Выделенный дизайн для объявлений
- Баннер или бейдж "ОБЪЯВЛЕНИЕ"
- Возможность прикрепления важных вложений
- Уведомления о новых объявлениях (push, email опционально)

### Разрешения
- `VIEW` - просмотр канала
- `PUBLISH_ANNOUNCEMENTS` - публикация объявлений
- `MANAGE_MESSAGES` - управление объявлениями
- `MANAGE_CHANNEL` - управление каналом
- `MANAGE_PERMISSIONS` - управление правами

### Модель данных (требуется)
```python
class AnnouncementCrossPost(Base):
    """Связь между оригинальным объявлением и его копиями в других каналах."""
    __tablename__ = "announcement_cross_posts"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    original_message_id: Mapped[int] = mapped_column(ForeignKey("messages.id"), nullable=False)
    cross_posted_message_id: Mapped[int] = mapped_column(ForeignKey("messages.id"), nullable=False)
    target_channel_id: Mapped[int] = mapped_column(ForeignKey("channels.id"), nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    
    original_message: Mapped["Message"] = relationship(foreign_keys=[original_message_id])
    cross_posted_message: Mapped["Message"] = relationship(foreign_keys=[cross_posted_message_id])
    target_channel: Mapped["Channel"] = relationship()
```

### Особенности
- По умолчанию только модераторы могут публиковать
- Обычные пользователи могут только читать
- Сообщения не могут быть удалены обычными пользователями
- Поддержка форматирования (markdown, rich text)
- Возможность планирования публикации (опционально)

### Статус
❌ **Не реализовано** - требуется разработка

---

## 5. FORUM CHANNELS (Форумные каналы)

### Назначение
Каналы для структурированных обсуждений в формате форума. Каждое сообщение становится отдельной темой (post), к которой можно отвечать. Поддерживает теги, сортировку, модерацию.

### Планируемый функционал ❌

#### Базовый функционал
- Создание постов (тем обсуждения)
- Ответы на посты
- Редактирование и удаление постов
- Закрепление постов
- Архивация постов
- Теги для постов
- Реакции на посты

#### Структура форума
- Список постов (тем) в канале
- Каждый пост имеет:
  - Заголовок
  - Автор
  - Количество ответов
  - Последний ответ
  - Теги
  - Статус (активен, закреплен, архивирован)
- Сортировка постов:
  - По дате создания
  - По дате последнего ответа
  - По популярности (количество ответов)
  - По активности

#### Модерация
- Модерация постов (одобрение/отклонение)
- Управление тегами
- Удаление постов
- Блокировка постов
- Перенос постов в другие каналы

#### UI/UX особенности
- Представление в виде списка постов
- Клик по посту открывает детальный вид с ответами
- Фильтрация по тегам
- Поиск по постам
- Пагинация

### Разрешения
- `VIEW` - просмотр канала
- `CREATE_FORUM_POSTS` - создание постов
- `MODERATE_FORUM_POSTS` - модерация постов
- `MANAGE_MESSAGES` - управление сообщениями
- `MANAGE_CHANNEL` - управление каналом
- `MANAGE_PERMISSIONS` - управление правами

### Модель данных (требуется)
```python
class ForumPost(Base):
    """Пост в форумном канале."""
    __tablename__ = "forum_posts"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    channel_id: Mapped[int] = mapped_column(ForeignKey("channels.id"), nullable=False)
    message_id: Mapped[int] = mapped_column(ForeignKey("messages.id"), nullable=False, unique=True)
    title: Mapped[str] = mapped_column(String(256), nullable=False)
    author_id: Mapped[int] = mapped_column(ForeignKey("users.id"), nullable=False)
    is_pinned: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    is_archived: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    is_locked: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    reply_count: Mapped[int] = mapped_column(Integer, default=0, nullable=False)
    last_reply_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    last_reply_by_id: Mapped[int | None] = mapped_column(ForeignKey("users.id"))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
    
    channel: Mapped["Channel"] = relationship()
    message: Mapped["Message"] = relationship()
    author: Mapped["User"] = relationship(foreign_keys=[author_id])
    last_reply_by: Mapped["User | None"] = relationship(foreign_keys=[last_reply_by_id])
    tags: Mapped[list["ForumPostTag"]] = relationship(back_populates="post", cascade="all, delete-orphan")

class ForumPostTag(Base):
    """Тег для поста в форуме."""
    __tablename__ = "forum_post_tags"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    post_id: Mapped[int] = mapped_column(ForeignKey("forum_posts.id", ondelete="CASCADE"), nullable=False)
    tag_name: Mapped[str] = mapped_column(String(64), nullable=False)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    
    post: Mapped["ForumPost"] = relationship(back_populates="tags")
    
    __table_args__ = (UniqueConstraint("post_id", "tag_name", name="uq_forum_post_tag"),)

class ForumChannelTag(Base):
    """Предустановленные теги для форумного канала."""
    __tablename__ = "forum_channel_tags"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    channel_id: Mapped[int] = mapped_column(ForeignKey("channels.id", ondelete="CASCADE"), nullable=False)
    name: Mapped[str] = mapped_column(String(64), nullable=False)
    color: Mapped[str] = mapped_column(String(7), nullable=False, default="#99AAB5")  # HEX color
    emoji: Mapped[str | None] = mapped_column(String(32))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    
    channel: Mapped["Channel"] = relationship()
    
    __table_args__ = (UniqueConstraint("channel_id", "name", name="uq_forum_channel_tag"),)
```

### Особенности
- Первое сообщение в канале становится постом (темой)
- Последующие сообщения в канале - ответы на пост
- Поддержка тегов для категоризации
- Возможность закрепления важных постов
- Архивация старых постов
- Модерация новых постов (опционально)

### Статус
❌ **Не реализовано** - требуется разработка

---

## 6. EVENT CHANNELS (Каналы событий)

### Назначение
Каналы для создания и управления событиями (мероприятиями). Поддерживает создание событий с датой, временем, описанием, участниками, напоминаниями.

### Планируемый функционал ❌

#### Базовый функционал
- Создание событий
- Редактирование событий
- Удаление событий
- Просмотр списка событий
- Календарное представление
- Фильтрация по статусу (предстоящие, прошедшие, отмененные)

#### Структура события
- Название события
- Описание
- Дата и время начала
- Дата и время окончания (опционально)
- Местоположение (опционально)
- Организатор
- Участники (RSVP: да, нет, возможно)
- Статус (запланировано, идет, завершено, отменено)
- Изображение/баннер (опционально)
- Ссылка на внешний ресурс (опционально)

#### Управление участниками
- Приглашение участников
- RSVP (подтверждение участия)
- Список участников
- Список заинтересованных
- Уведомления о событии

#### Напоминания
- Автоматические напоминания перед событием
- Настраиваемые интервалы (1 час, 1 день, 1 неделя)
- Push-уведомления
- Email-уведомления (опционально)

#### UI/UX особенности
- Календарное представление
- Список событий
- Детальный вид события
- Интеграция с календарем (iCal, Google Calendar)
- Экспорт события

### Разрешения
- `VIEW` - просмотр канала
- `CREATE_EVENTS` - создание событий
- `MANAGE_EVENTS` - управление событиями (редактирование, удаление)
- `MANAGE_CHANNEL` - управление каналом
- `MANAGE_PERMISSIONS` - управление правами

### Модель данных (требуется)
```python
class Event(Base):
    """Событие в канале событий."""
    __tablename__ = "events"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    channel_id: Mapped[int] = mapped_column(ForeignKey("channels.id"), nullable=False)
    message_id: Mapped[int | None] = mapped_column(ForeignKey("messages.id"), nullable=True)
    title: Mapped[str] = mapped_column(String(256), nullable=False)
    description: Mapped[str | None] = mapped_column(Text)
    organizer_id: Mapped[int] = mapped_column(ForeignKey("users.id"), nullable=False)
    start_time: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    end_time: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    location: Mapped[str | None] = mapped_column(String(512))
    image_url: Mapped[str | None] = mapped_column(String(512))
    external_url: Mapped[str | None] = mapped_column(String(512))
    status: Mapped[str] = mapped_column(String(32), default="scheduled", nullable=False)  # scheduled, ongoing, completed, cancelled
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now(), onupdate=func.now())
    
    channel: Mapped["Channel"] = relationship()
    message: Mapped["Message | None"] = relationship()
    organizer: Mapped["User"] = relationship(foreign_keys=[organizer_id])
    participants: Mapped[list["EventParticipant"]] = relationship(back_populates="event", cascade="all, delete-orphan")
    reminders: Mapped[list["EventReminder"]] = relationship(back_populates="event", cascade="all, delete-orphan")

class EventParticipant(Base):
    """Участник события."""
    __tablename__ = "event_participants"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    event_id: Mapped[int] = mapped_column(ForeignKey("events.id", ondelete="CASCADE"), nullable=False)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    rsvp_status: Mapped[str] = mapped_column(String(16), default="interested", nullable=False)  # yes, no, maybe, interested
    joined_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    
    event: Mapped["Event"] = relationship(back_populates="participants")
    user: Mapped["User"] = relationship()
    
    __table_args__ = (UniqueConstraint("event_id", "user_id", name="uq_event_participant"),)

class EventReminder(Base):
    """Напоминание о событии."""
    __tablename__ = "event_reminders"
    
    id: Mapped[int] = mapped_column(primary_key=True)
    event_id: Mapped[int] = mapped_column(ForeignKey("events.id", ondelete="CASCADE"), nullable=False)
    user_id: Mapped[int] = mapped_column(ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    reminder_time: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False)
    sent: Mapped[bool] = mapped_column(Boolean, default=False, nullable=False)
    sent_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    
    event: Mapped["Event"] = relationship(back_populates="reminders")
    user: Mapped["User"] = relationship()
    
    __table_args__ = (UniqueConstraint("event_id", "user_id", "reminder_time", name="uq_event_reminder"),)
```

### Особенности
- Интеграция с календарем (iCal, Google Calendar)
- Автоматические напоминания
- RSVP система
- Статусы событий
- Возможность отмены событий
- История событий

### Статус
❌ **Не реализовано** - требуется разработка

---

## План реализации

### Фаза 1: ANNOUNCEMENT CHANNELS (Приоритет: Высокий)

#### Задачи:
1. **Backend: Модели и миграции**
   - Создать модель `AnnouncementCrossPost`
   - Миграция для новой таблицы
   - Обновить модель `Message` для поддержки cross-posting

2. **Backend: API endpoints**
   - `POST /channels/{channel_id}/announcements` - создать объявление
   - `POST /channels/{channel_id}/announcements/{message_id}/cross-post` - опубликовать в другие каналы
   - `GET /channels/{channel_id}/announcements` - список объявлений
   - Проверка прав `PUBLISH_ANNOUNCEMENTS`

3. **Backend: Логика cross-posting**
   - Создание копий сообщений в целевых каналах
   - Синхронизация реакций
   - Управление связями между оригиналом и копиями

4. **Frontend: UI для объявлений**
   - Выделенный дизайн для объявлений
   - Кнопка "Опубликовать в другие каналы"
   - Выбор каналов для публикации
   - Отображение источника объявления

**Оценка времени**: 16-20 часов

---

### Фаза 2: FORUM CHANNELS (Приоритет: Высокий)

#### Задачи:
1. **Backend: Модели и миграции**
   - Создать модель `ForumPost`
   - Создать модель `ForumPostTag`
   - Создать модель `ForumChannelTag`
   - Миграции для новых таблиц
   - Индексы для производительности

2. **Backend: API endpoints**
   - `POST /channels/{channel_id}/posts` - создать пост
   - `GET /channels/{channel_id}/posts` - список постов (с пагинацией, сортировкой, фильтрацией)
   - `GET /channels/{channel_id}/posts/{post_id}` - получить пост с ответами
   - `PATCH /channels/{channel_id}/posts/{post_id}` - обновить пост
   - `DELETE /channels/{channel_id}/posts/{post_id}` - удалить пост
   - `POST /channels/{channel_id}/posts/{post_id}/pin` - закрепить пост
   - `POST /channels/{channel_id}/posts/{post_id}/archive` - архивировать пост
   - `POST /channels/{channel_id}/tags` - создать тег канала
   - `GET /channels/{channel_id}/tags` - список тегов канала

3. **Backend: Логика форума**
   - Преобразование первого сообщения в пост
   - Подсчет ответов
   - Обновление времени последнего ответа
   - Модерация постов

4. **Frontend: UI для форума**
   - Представление списка постов
   - Детальный вид поста с ответами
   - Создание нового поста
   - Фильтрация и сортировка
   - Управление тегами

**Оценка времени**: 24-30 часов

---

### Фаза 3: EVENT CHANNELS (Приоритет: Средний)

#### Задачи:
1. **Backend: Модели и миграции**
   - Создать модель `Event`
   - Создать модель `EventParticipant`
   - Создать модель `EventReminder`
   - Миграции для новых таблиц
   - Индексы для производительности

2. **Backend: API endpoints**
   - `POST /channels/{channel_id}/events` - создать событие
   - `GET /channels/{channel_id}/events` - список событий (с фильтрацией по статусу, дате)
   - `GET /channels/{channel_id}/events/{event_id}` - получить событие
   - `PATCH /channels/{channel_id}/events/{event_id}` - обновить событие
   - `DELETE /channels/{channel_id}/events/{event_id}` - удалить/отменить событие
   - `POST /channels/{channel_id}/events/{event_id}/rsvp` - изменить RSVP статус
   - `GET /channels/{channel_id}/events/{event_id}/participants` - список участников
   - `GET /channels/{channel_id}/events/calendar.ics` - экспорт в iCal

3. **Backend: Логика событий**
   - Валидация дат и времени
   - Автоматическое обновление статусов (scheduled -> ongoing -> completed)
   - Система напоминаний (фоновый процесс)
   - RSVP управление

4. **Backend: Система напоминаний**
   - Фоновый процесс для отправки напоминаний
   - Интеграция с системой уведомлений
   - Поддержка разных типов уведомлений (push, email)

5. **Frontend: UI для событий**
   - Календарное представление
   - Список событий
   - Создание/редактирование события
   - Детальный вид события
   - RSVP интерфейс
   - Экспорт в календарь

**Оценка времени**: 32-40 часов

---

## Общие задачи для всех типов каналов

### Backend
1. Обновить валидацию типов каналов при создании
2. Обновить фильтрацию каналов по типу
3. Обновить workspace events для новых типов
4. Добавить тесты для каждого типа канала

### Frontend
1. Обновить `CreateChannelDialog` для выбора типа канала
2. Обновить `ChannelSidebar` для отображения разных типов
3. Создать специализированные компоненты для каждого типа
4. Обновить роутинг для поддержки новых типов

---

## Приоритизация

### MVP (Минимально жизнеспособный продукт)
1. ✅ TEXT CHANNELS - уже реализованы
2. ✅ VOICE CHANNELS - уже реализованы
3. ✅ STAGE CHANNELS - уже реализованы
4. 🔄 ANNOUNCEMENT CHANNELS - высокий приоритет
5. 🔄 FORUM CHANNELS - высокий приоритет

### Вторая итерация
6. 🔄 EVENT CHANNELS - средний приоритет

### Дополнительные улучшения
7. Расширенные функции для каждого типа
8. Интеграции с внешними сервисами
9. Аналитика и статистика

---

## Технические детали

### Общие принципы
- Каждый тип канала использует базовую модель `Channel`
- Специфичный функционал реализуется через связанные модели
- Разрешения контролируют доступ к функциям
- WebSocket события для real-time обновлений

### Производительность
- Индексы на часто используемые поля
- Пагинация для больших списков
- Кэширование при необходимости
- Оптимизация запросов

### Безопасность
- Проверка прав на всех уровнях
- Валидация входных данных
- Защита от SQL injection
- Rate limiting для API

---

## Метрики успеха

1. ✅ TEXT, VOICE, STAGE каналы работают стабильно
2. 🔄 ANNOUNCEMENT каналы поддерживают cross-posting
3. 🔄 FORUM каналы поддерживают структурированные обсуждения
4. 🔄 EVENT каналы поддерживают создание и управление событиями
5. Все типы каналов имеют интуитивный UI
6. Производительность не ухудшилась
7. Покрытие тестами > 80%

---

## Дальнейшие улучшения (не в текущем плане)

1. **Интеграция с внешними календарями** - синхронизация событий
2. **Автоматическая модерация** - AI-модерация для форумов
3. **Аналитика каналов** - статистика использования
4. **Шаблоны каналов** - предустановленные настройки
5. **Интеграция с ботами** - автоматизация через ботов
6. **Мобильные уведомления** - push для всех типов каналов
7. **Экспорт данных** - экспорт форумов, событий в различные форматы


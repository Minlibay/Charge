# Следующие шаги по реализации SFU

## ✅ Что уже сделано

### Этап 1: SFU сервер ✅
- ✅ Создана структура `sfu-server/`
- ✅ Реализован базовый Mediasoup сервер
- ✅ Создан Dockerfile
- ✅ Обновлен `docker-compose.yml`

### Этап 2: Интеграция с бэкендом ✅
- ✅ Создан `backend/app/services/sfu_manager.py`
- ✅ Обновлен `backend/app/config.py` с настройками SFU
- ✅ Обновлен `VoiceSignalManager` для автоматического создания/удаления SFU комнат
- ✅ Добавлены API endpoints в `backend/app/api/voice.py`
- ✅ Обновлен `backend/app/api/config.py` для экспорта SFU настроек

## 🔧 Что нужно сделать ВРУЧНУЮ

### 1. Установить зависимости SFU сервера

```bash
cd sfu-server
npm install
```

### 2. Добавить переменные в `.env`

```env
# SFU Configuration
SFU_ENABLED=true
SFU_SERVER_URL=http://sfu:3000
SFU_WS_URL=ws://sfu:3001
SFU_API_KEY=your-secret-api-key-change-this
SFU_FEATURE_FLAG_ENABLED=true
```

**Важно:** `SFU_API_KEY` должен совпадать с тем, что в SFU сервере! Отключить SFU при необходимости можно установив `SFU_ENABLED=false`.

### 3. Установить httpx в бэкенде

```bash
cd backend
poetry add httpx
# или если используете pip
pip install httpx
```

### 4. Собрать и запустить SFU

```bash
# Из корня проекта
docker-compose build sfu
docker-compose up -d sfu

# Проверить логи
docker-compose logs -f sfu
```

### 5. Проверить работу

```bash
# Health check
curl http://localhost:3000/health

# Должен вернуть: {"status":"ok","timestamp":"..."}
```

## 📋 Следующие этапы (будут выполнены автоматически)

### Этап 3: Обновление клиента
- [x] Рефакторинг `VoiceClient.ts`
- [x] Создание `SFUVoiceClient.ts`
- [x] Интеграция с UI

### Этап 4: Тестирование
- [ ] Базовое тестирование
- [ ] Нагрузочное тестирование
- [ ] Оптимизация

## 🚀 Готовы продолжить?

После выполнения шагов 1-5, сообщите мне, и я продолжу с реализацией клиентской части!

